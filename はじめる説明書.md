# ふたりの図鑑アプリ：はじめる説明書

これは無料でスマホのホーム画面に入れられるアプリです。アプリストアは使いません。

## まず、二人で決めること

図鑑用のメールアドレスとパスワードを一つ決めます。二人とも同じものを使います。たとえば、図鑑専用に新しいメールアドレスを作ると分かりやすいです。このメールアドレスが、二人だけの「図鑑アカウント」になります。

## 最初の一回だけすること（大人と一緒に）

1. [Supabase](https://supabase.com/) で無料アカウントを作ります。
2. 新しいプロジェクトを一つ作ります。名前は `futari-zukan` などでOKです。
3. [supabase-setup.sql](./supabase-setup.sql) を開き、書いてある文字を全部コピーします。
4. Supabase の **SQL Editor** を開き、コピーした文字を貼って **Run** を押します。途中でエラーが出た場合も、最新版のSQLを貼り直してもう一度 **Run** を押せば大丈夫です。
5. 次の「数字と文字をコピーする」をします。
6. 次の「アプリへ貼る」をします。
7. 次の「インターネットへ公開する」をします。公開されたURLが、二人の図鑑アプリの住所です。

## 5. 数字と文字をコピーする（Supabase）

1. Supabaseの左下にある歯車 **Project Settings** を押します。
2. 左の一覧から **API Keys** を押します。古い画面では **API** と表示されることがあります。
3. 画面上部の **Project URL** の右にあるコピーのボタンを押します。`https://...supabase.co` で始まる文字です。メモ帳へ貼っておきます。
4. その下の **Publishable key** をコピーします。`sb_publishable_` で始まる文字です。もし見当たらなければ、**Legacy API Keys** の `anon` をコピーしても大丈夫です。

**絶対にコピーしてはいけないもの：** `Secret key`、`service_role`、`sb_secret_`。名前に `secret` または `service` がある鍵は、誰にも見せてはいけません。

## 6. アプリへ貼る（config.js）

1. このフォルダの [config.js](./config.js) を右クリックします。
2. **メモ帳で編集** を押します。（出ないときは **プログラムから開く → メモ帳**。）
3. 次の3か所だけを書き換えます。`' '` の中へ貼ります。

```js
window.FUTARI_DEX_CONFIG = {
  supabaseUrl: 'ここへ Project URL を貼る',
  supabaseAnonKey: 'ここへ Publishable key または anon key を貼る',
  room: 'ふたりだけの合言葉',
  members: 'みお & しゅう'
};
```

4. `room` は、二人だけに分かる長めの合言葉に変えます。例：`mio-shu-2026-pink-47`。二人は同じアプリURLを使うため、ここは一度決めたら変えません。
5. `members` はアプリに表示する二人の名前です。好きな名前に変えて大丈夫です。
6. 上の **ファイル → 上書き保存** を押して、メモ帳を閉じます。

## 7. インターネットへ公開する（Netlify Drop・いちばん簡単）

1. パソコンで [Netlify Drop](https://app.netlify.com/drop) を開きます。
2. Googleなどで無料のNetlifyアカウントを作り、ログインします。
3. エクスプローラーで、この図鑑の `outputs` フォルダを開きます。中に `index.html` が見えていることを確かめます。
4. `outputs` **フォルダそのもの**を、Netlifyの点線の大きな四角へドラッグ＆ドロップします。ファイルを一つずつではなく、フォルダをまとめて入れます。
5. 少し待つと `https://なにか.netlify.app` というURLが表示されます。それをコピーして、二人のLINEなどへ送ります。
6. そのURLをスマホで開き、説明書の「スマホへアプリとして入れる」をします。

あとで内容を変更したときも、同じNetlify画面の **Deploys** を開き、更新後の `outputs` フォルダをもう一度ドラッグ＆ドロップすれば更新できます。

## スマホへアプリとして入れる

### iPhone

1. Safari で図鑑のURLを開きます。
2. 画面下の **共有** ボタンを押します。
3. **ホーム画面に追加** を押し、**追加** を押します。

### Android

1. Chrome で図鑑のURLを開きます。
2. 右上の **︙** を押します。
3. **アプリをインストール** または **ホーム画面に追加** を押します。

ホーム画面の赤いモンスターボールのアイコンを押すと、アプリのように開けます。

## 二人で使う

二人とも同じ公開URLを開き、右上の丸いボタンから、最初に決めた同じメールアドレス・パスワードでログインします。最初の人は **はじめての人はこちら** を押してアカウントを作ります。確認メールが来たときは、そのメールのリンクを一度押してください。

写真を登録したあと、相手の画面を一度下へ引っぱって更新すると、同じ図鑑を確認できます。

困ったら、まずアプリを閉じてもう一度開いてください。それでも直らない場合は、二人が同じURLを使っているか確認します。
